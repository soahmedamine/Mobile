import 'package:flutter/material.dart';

class MapExplorerScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Explorer la carte')),
      body: Center(child: Text('Carte interactive - À implémenter')),
    );
  }
}