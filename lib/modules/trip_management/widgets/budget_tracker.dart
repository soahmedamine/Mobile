import 'package:flutter/material.dart';

class BudgetTracker extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(child: Text('Tracker budget - À implémenter'));
  }
}